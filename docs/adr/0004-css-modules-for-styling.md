# CSS Modules for component styling

CSS Modules are used for all component styling instead of Tailwind CSS or plain global CSS. Tailwind was explicitly rejected: it is considered complex, difficult to read, and hard to maintain. CSS Modules provide scoped, co-located styles with plain CSS syntax — simple to understand, easy to maintain, and familiar to any CSS developer. Plain global CSS was ruled out because it becomes hard to manage as components multiply.
