# React as the UI framework

React is used instead of staying with vanilla TypeScript, despite the app's small scope, because the primary developer is personally familiar with React and values the component model for maintainability. The bundle overhead (~45 KB runtime) is acceptable for a web app targeting devices with a network connection. Svelte or vanilla TS would be leaner but would slow development.
